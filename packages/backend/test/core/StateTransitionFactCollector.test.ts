import { StateTransitionFactCollector } from '../../src/core/StateTransitionFactCollector'

describe(StateTransitionFactCollector.name, () => {
  it('parses logs, saves and returns records', () => {
    // @todo
  })
})
