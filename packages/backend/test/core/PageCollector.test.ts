import { PageCollector } from '../../src/core/PageCollector'

describe(PageCollector.name, () => {
  it('fetches memory page logs and transactions and then saves records to repository', () => {
    // @todo
  })
})
