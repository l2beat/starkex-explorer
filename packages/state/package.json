{
  "name": "@explorer/state",
  "version": "0.1.0",
  "private": true,
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "sideEffects": false,
  "engines": {
    "node": "^16.0.0",
    "yarn": ">=1.22.0"
  },
  "scripts": {
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "lint": "eslint --ext .ts --max-warnings 0 src",
    "lint:fix": "yarn lint --fix",
    "typecheck": "tsc --noEmit",
    "prebuild": "yarn clean",
    "build": "swc --no-swcrc --source-maps --no-swcrc src -d build",
    "clean": "rm -rf build",
    "test": "mocha"
  },
  "dependencies": {
    "@explorer/crypto": "*",
    "@explorer/encoding": "*",
    "@explorer/types": "*",
    "@types/lodash": "^4.14.182",
    "lodash": "^4.17.21"
  },
  "devDependencies": {}
}
